{
  "name": "jev-lab",
  "version": "0.2.0",
  "private": true,
  "description": "Jev use-case lab: Hundred society sim and Jev Shogi",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @lab/web --filter @hundred/server --filter @hundred/web --filter @shogi/server --filter @shogi/web --parallel dev",
    "dev:lab": "pnpm --filter @lab/web dev",
    "dev:hundred": "pnpm --filter @hundred/server --filter @hundred/web --parallel dev",
    "dev:shogi": "pnpm --filter @shogi/server --filter @shogi/web --parallel dev",
    "build": "pnpm -r --if-present build",
    "start": "pnpm --filter @hundred/server start",
    "lint": "oxlint --type-aware",
    "lint:fix": "oxlint --type-aware --fix",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "typecheck": "tsc -b --pretty",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "pnpm --filter @hundred/web test:e2e",
    "test:jev": "vitest run --config packages/decision-jev/vitest.jev.config.ts",
    "deadcode": "knip",
    "replay": "pnpm --filter @hundred/server replay",
    "sprite:key": "node scripts/sprite-key.mjs",
    "boundaries": "tsx scripts/check-boundaries.ts",
    "prepare": "simple-git-hooks",
    "check": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm test && pnpm boundaries && pnpm deadcode && pnpm build"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "knip": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "oxlint-tsgolint": "catalog:",
    "pngjs": "^7.0.0",
    "simple-git-hooks": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "simple-git-hooks": {
    "pre-commit": "pnpm exec oxfmt && pnpm exec oxlint --type-aware"
  },
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@12.4.2"
}
